<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  
  <!-- @stylesheet normalize safira -->
  <%= stylesheet_link_tag "safira", :media => "all" %>
  <!-- @javascript jquery-1.8.3.min app -->
  <%= javascript_include_tag "application" %>

  <link href='http://fonts.googleapis.com/css?family=Source+Sans+Pro:400,600,700' rel='stylesheet' type='text/css'>

  <link href="<!-- @path favicon.ico -->" rel="shortcut icon">
  <link href="<!-- @path apple-touch-icon.png -->" rel="apple-touch-icon">
  
  <title><%= content_for?(:title) ? "Safira | " + yield(:title) : "Safira - Agilizando sua Empresa" %></title>
  
  <%= csrf_meta_tag %>
</head>
<body class="sidebar-push-ready">

  <%= render "partials/header" %>
  <%= render "partials/subheader" %>
  
  <div class="wrapper">
  	<%= yield %>
  </div>
</body>
</html>
